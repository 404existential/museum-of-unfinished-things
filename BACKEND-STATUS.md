## Backend status

The repository now contains the Supabase schema and browser configuration template.

The remaining setup is intentionally one-time: create a Supabase project, run `supabase.sql`, disable email confirmation, then put the Project URL and publishable/anon key into `config.js`.

After that, GitHub Pages serves the same public archive to everyone, with username/password accounts and persistent public deposits.
